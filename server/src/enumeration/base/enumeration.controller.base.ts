/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { EnumerationService } from "../enumeration.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { EnumerationCreateInput } from "./EnumerationCreateInput";
import { EnumerationWhereInput } from "./EnumerationWhereInput";
import { EnumerationWhereUniqueInput } from "./EnumerationWhereUniqueInput";
import { EnumerationFindManyArgs } from "./EnumerationFindManyArgs";
import { EnumerationUpdateInput } from "./EnumerationUpdateInput";
import { Enumeration } from "./Enumeration";
@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class EnumerationControllerBase {
  constructor(
    protected readonly service: EnumerationService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "Enumeration",
    action: "create",
    possession: "any",
  })
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Enumeration })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: EnumerationCreateInput
  ): Promise<Enumeration> {
    return await this.service.create({
      data: data,
      select: {
        category: true,
        createdAt: true,
        id: true,
        isActive: true,
        name: true,
        updatedAt: true,
        value: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "Enumeration",
    action: "read",
    possession: "any",
  })
  @common.Get()
  @swagger.ApiOkResponse({ type: [Enumeration] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(EnumerationFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<Enumeration[]> {
    const args = plainToClass(EnumerationFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        category: true,
        createdAt: true,
        id: true,
        isActive: true,
        name: true,
        updatedAt: true,
        value: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "Enumeration",
    action: "read",
    possession: "own",
  })
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Enumeration })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: EnumerationWhereUniqueInput
  ): Promise<Enumeration | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        category: true,
        createdAt: true,
        id: true,
        isActive: true,
        name: true,
        updatedAt: true,
        value: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "Enumeration",
    action: "update",
    possession: "any",
  })
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Enumeration })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: EnumerationWhereUniqueInput,
    @common.Body() data: EnumerationUpdateInput
  ): Promise<Enumeration | null> {
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          category: true,
          createdAt: true,
          id: true,
          isActive: true,
          name: true,
          updatedAt: true,
          value: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @nestAccessControl.UseRoles({
    resource: "Enumeration",
    action: "delete",
    possession: "any",
  })
  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Enumeration })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: EnumerationWhereUniqueInput
  ): Promise<Enumeration | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          category: true,
          createdAt: true,
          id: true,
          isActive: true,
          name: true,
          updatedAt: true,
          value: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
